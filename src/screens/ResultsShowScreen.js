import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
const ResultsShowScreen = () => {
  return <Text>Results Show</Text>;
};

const styles = StyleSheet.create({});

export default ResultsShowScreen;
